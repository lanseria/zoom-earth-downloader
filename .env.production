# --- Downloader Configuration ---

# 要下载的卫星 (例如: himawari, goes-east, etc.)
SATELLITE=himawari

# 下载的 Zoom 等级范围 (包含)
MIN_ZOOM=4
MAX_ZOOM=7

# 下载任务的执行周期（小时）
CHECK_INTERVAL_HOURS=1

# 并发下载线程数
CONCURRENCY=20

# ----------------- 新增/修改部分开始 -----------------

# 定义在 主机 (Host) 上的数据存储绝对路径
# Docker Compose 将会读取这个路径并用它来设置卷映射
# 请确保这个目录存在，并且Docker有权限写入
HOST_DATA_PATH=/home/bmc-r1/share/data/zoom-earth-tiles

# ----------------- 新增/修改部分结束 -----------------

# 瓦片在容器内的存储路径 (这个通常不需要修改)
# 这个路径会通过 docker-compose.yml 映射到主机
DOWNLOAD_PATH=/data/tiles

CLEANUP_DAYS=30