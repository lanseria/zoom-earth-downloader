# --- Downloader Configuration ---

# 要下载的卫星 (例如: himawari, goes-east, etc.)
SATELLITE=himawari

# 下载的 Zoom 等级范围 (包含)
MIN_ZOOM=1
MAX_ZOOM=7

# 下载任务的执行周期（小时）
CHECK_INTERVAL_HOURS=3

# 并发下载线程数
CONCURRENCY=10

# 瓦片在容器内的存储路径 (通常不需要修改)
# 这个路径会通过 docker-compose.yml 映射到主机
DOWNLOAD_PATH=/data/tiles

CLEANUP_DAYS=30